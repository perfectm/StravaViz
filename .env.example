# Strava API Configuration
# Get these values from https://developers.strava.com/
STRAVA_CLIENT_ID=your_client_id_here
STRAVA_CLIENT_SECRET=your_client_secret_here
STRAVA_REFRESH_TOKEN=your_refresh_token_here

# OAuth Configuration (for multi-user authentication)
# Set this to your application URL + /auth/callback
# Development: http://localhost:8002/auth/callback
# Production: https://yourdomain.com/auth/callback
OAUTH_REDIRECT_URI=http://localhost:8002/auth/callback

# Session Security
# Generate a secure random string for production: python -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET=change_this_to_a_random_secret_in_production

# Cookie Security (set to true in production with HTTPS)
COOKIE_SECURE=false

# Optional: Club ID for club dashboard
# Find your club ID in the URL when viewing your club on Strava: strava.com/clubs/CLUB_ID
STRAVA_CLUB_ID=your_club_id_here